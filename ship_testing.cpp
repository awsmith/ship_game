#include <iostream>
#include "utils.h"
#include "Ship.h"

int main()
{
  Ship tempShip;
  //SDL_Rect tempCoords = tempShip.get_coords();
  //std::cout << "tempShip coords:\nX: " << tempCoords.x;
  //std::vector<Ship> ships;

  return 0;
}
